<UserControl x:Class="WpfApplication1.Views.Templates.TextBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApplication1.Views.Templates"
             mc:Ignorable="d">

    <!--x:Name="{Binding Id ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"-->
    <StackPanel Orientation="Horizontal" Grid.ColumnSpan="{Binding Size ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        <Label Content="{Binding Caption ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox MinWidth="50" Text="{Binding BindedData ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </StackPanel>
</UserControl>
